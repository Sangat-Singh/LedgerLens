// Tailwind's PostCSS plugin is enabled after dependencies are installed.
// Keeping this config dependency-free lets the initial shell run in restricted environments.
export default { plugins: {} };
